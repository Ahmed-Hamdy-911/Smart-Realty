import 'package:flutter/material.dart';
import 'package:smart_realty/core/widgets/custom_text.dart';

class PropertiesOnMapView extends StatelessWidget {
  const PropertiesOnMapView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child:  CustomText(text: "Not implemented yet"),
    );
  }
}